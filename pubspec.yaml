name: campus_app
description: Kampüs uygulaması - Topluluklar, forum, marketplace ve daha fazlası
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  intl: ^0.18.0
  provider: ^6.0.5
  image_picker: ^1.0.4
  file_picker: ^8.0.0
  http: ^1.6.0
  shared_preferences: ^2.5.3
  animations: ^2.1.1
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/data/
